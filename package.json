{
  "name": "index",
  "version": "1.0.0",
  "description": "this is just directory",
  "main": "index.js",
  "scripts": {
    "start": "npm run lite",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lite": "lite-server"
  },
  "author": "waleed taib",
  "license": "ISC",
  "devDependencies": {
    "lite-server": "^2.6.1"
  }
}
